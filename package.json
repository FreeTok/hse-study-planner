{
  "name": "study-planner",
  "version": "0.1.0",
  "description": "Small web app that creates a study plan before a deadline.",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "check": "node --check src/server.js && node --check src/planner.js && node --check public/app.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
